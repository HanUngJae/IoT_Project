Wemos D1 R1

D4 = trig핀
D5 = echo핀
D3 = relay핀

serial_multitap(시리얼통신을 이용한 멀티탭제어)
mqtt_multitap(mqtt를 이용한 멀티탭제어)

mqtt로 on/off/auto제어가능

* 아래는 미완성 *
auto를 off 할때 serial 통신에서는 가능하지만 mqtt통신에서는 불가능.
스마트폰에서 받아온 거리에 따른 제어를 아직 구현 못함(현재 30cm로 고정)